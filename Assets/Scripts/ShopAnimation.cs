using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ShopAnimation : MonoBehaviour
{/*
    float closedShopPosX = -187f;
    float openShopPosX = 138.7f;

    public int speed = 10;

    public GameObject player;

    // Start is called before the first frame update
    void Start()
    {
        transform.position = new Vector2(closedShopPosX, transform.position.y);
    }

    // Update is called once per frame
    void Update()
    {
        if(player.GetComponent<Player>().isInShop && transform.position.x < openShopPosX)
        {
            transform.position = new Vector3(transform.position.x + speed * Time.deltaTime, transform.position.y, transform.position.z);
        }
        else if(!player.GetComponent<Player>().isInShop && transform.position.x > closedShopPosX)
        {
            transform.position = new Vector3(transform.position.x - speed * Time.deltaTime, transform.position.y, transform.position.z);
        }
    }
*/}
